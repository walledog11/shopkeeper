{
  "buildCommand": "npx prisma generate --schema=packages/db/prisma/schema.prisma && npm run build -w packages/db && npm run build -w packages/agent && npm run build -w packages/integrations && npx turbo run build --filter=shopkeeper-dashboard",
  "outputDirectory": "apps/dashboard/.next",
  "installCommand": "npm install --ignore-scripts && npm run build -w @shopkeeper/shared && npm rebuild @shopkeeper/db",
  "framework": "nextjs"
}

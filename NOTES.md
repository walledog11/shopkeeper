# Eval gate notes

- Stage 1b is outstanding: the shared Anthropic client does not set explicit `timeout` or `maxRetries` options.
- The 2026-08-16 Step A run passed 87/87 tests, but Vitest's compact agent reporter discarded all passed-test console output, including `[eval:usage]`; `agent-audit-eval-gate` disables console interception and writes `test-results/eval-report.log`.
- Dashboard typecheck is currently blocked by unrelated working-tree changes: `useConversationAgentFlow.ts` omits required `AgentTurn.error`, and `walkthrough-briefing.unit.test.ts` supplies an optional `isEscalationOnly` where `WalkthroughItem` requires a boolean.

## 2026-08-16 session handoff

### Session commits

- `ca9acbf45d4e3206c85a13663cac1736f556e4ac` — Replace command palette with silent concierge navigation. Local: `master`, `agent-audit-eval-gate`. Remote: `origin/master`.
- `485cc836ecdeb888f60ec501c39f886e4aae10e1` — Guard planning against stale request intelligence. Local: `master`, `agent-audit-eval-gate`. Remote: `origin/master`.
- `88d568958916a672f91949e7df8a934e0e491541` — Block auto-replies when order prefetch fails. Local: `master`, `agent-audit-eval-gate`. Remote: `origin/master`.
- `35d52bdc2b18cf73f00d0c7f05ea8bccc13df6bf` — Share the current-request skip predicate. Local: `master`, `agent-audit-eval-gate`. Remote: `origin/master`.
- `02cafe0a9b166eebbbdc80a7c7a96416b3802a7b` — Test failed order prefetch blocks auto-reply. Local: `master`, `agent-audit-eval-gate`. Remote: `origin/master`.
- `dabd962f784612dde70da4e65692b8362d722209` — Guard request classifier signals against stale writes. Local: `master`, `agent-audit-eval-gate`. Remote: `origin/master`.
- `e7fec207f85fb22295bed8699324578836ccec85` — Align no-work gates with classifier scope. Local: `master`, `agent-audit-eval-gate`. Remote: `origin/master`.
- `7e17659ae04960243760923aa36490a3d2638ca2` — Separate eval comparison from baseline overwrite. Local: `agent-audit-eval-gate`. Remote: none.
- `d145e22cec9fc9a2b79dfe87f82a5fd27d94b19e` — Persist eval usage and fixture reports. Local: `agent-audit-eval-gate`. Remote: none.
- `2cc3341f5f18434b291697fe953db3d404c8010f` — Record eval usage by model. Local: `agent-audit-eval-gate`. Remote: none.
- `e9345501b7d944e69df2abd9baecef4de3f79a80` — Recapture the full agent eval baseline. Local: `master`. Remote: `origin/master`.

### Stranded on `agent-audit-eval-gate`

- `7e17659ae04960243760923aa36490a3d2638ca2` — non-destructive `test:evals:baseline` and explicit destructive `test:evals:baseline:overwrite` split.
- `d145e22cec9fc9a2b79dfe87f82a5fd27d94b19e` — `--disableConsoleIntercept`, durable `test-results/eval-report.log`, and per-fixture planner-iteration reporting.
- `2cc3341f5f18434b291697fe953db3d404c8010f` — per-model eval usage collection and `[eval:model-usage]` reporting.
- No remote branch contains these three commits.

### Outstanding

- Stage 1b: the shared Anthropic client has no explicit `timeout` or `maxRetries` options.
- The 1-hour stable-prefix cache TTL has not been verified with two plans separated by at least seven minutes and different thread contexts.
- `resolvePublishDecision` applies `shouldSkipRequestWork` only when `thread.requestSourceMessageId === identity.sourceMessageId`; precompute and off-hours auto-ack apply the predicate directly. A request-source mismatch skips at the earlier gates but permits publishing at the final gate.
- ~~No replacement eval capture was run after the checkout switched from `agent-audit-eval-gate` to `master` during the capture.~~ Closed 2026-08-16: a full 84-fixture, 3-repeat non-destructive run on `master`, with the eval-harness commits cherry-picked across, reproduced `e9345501` exactly — aggregate 247/252 (98.0%), same two advisory fixtures at the same pass counts, zero regressions, baseline checksum unchanged.
- The baseline in `e9345501` contains 84 records, 252 runs, a `usage` key, and per-model usage generated by harness code not present on `master`.
- `ca9acbf4` remains a mixed commit containing UI/concierge changes, the 1-hour cache TTL, Shopify prefetch work, lazy Anthropic client initialization, and gateway environment-loading changes; it was already pushed and was not split or rewritten.

### Found and not acted on

- Dashboard typecheck failures in dirty UI work: `useConversationAgentFlow.ts` omits required `AgentTurn.error`; `walkthrough-briefing.unit.test.ts` passes optional `isEscalationOnly` where `WalkthroughItem` requires `boolean`.
- Capture advisory result: `quick-reply-thanks-ack` passed `0/3`; runs classified `needs_review` after repeated `get_order_by_name` errors/escalation.
- Capture advisory result: `refund-over-cap-escalate` passed `1/3`; two runs created a refund instead of escalating. Execution safety remained enforced by the advisory fixture's separate guard.
- The capture process started with the `agent-audit-eval-gate` harness loaded and finished after the working checkout moved to `master`; `packages/agent` and `apps/gateway` were identical between the two tips, but five eval-harness files differed.
- `claude-sonnet-4-6` is absent from `LLM_PRICING` in `packages/db/llm-spend.ts`, so every call it makes falls through to `FALLBACK_PRICE` ($5/$25 per MTok vs. the real rate). The judge model in the eval harness runs on it, and the production daily-spend backstop overcounts every judge call. Deliberate direction of error — the comment says the backstop must never undercount — but it means `llm_daily_spend` is not an accurate cost figure for any org whose traffic touches that model.

### Eval scripts by branch

- `agent-audit-eval-gate`: `test:evals` and `test:evals:fixture` use `--disableConsoleIntercept`.
- `agent-audit-eval-gate`: `test:evals:baseline` runs three repeats without `UPDATE_EVAL_BASELINE`.
- `agent-audit-eval-gate`: `test:evals:baseline:overwrite` sets `UPDATE_EVAL_BASELINE=1` and invokes `test:evals:baseline`.
- `agent-audit-eval-gate`: the harness writes `test-results/eval-report.log`, reports planner iterations, and records per-model usage.
- `master` / `origin/master`: `test:evals` and `test:evals:fixture` do not use `--disableConsoleIntercept`.
- `master` / `origin/master`: `test:evals:baseline` hard-codes `EVAL_REPEATS=3 UPDATE_EVAL_BASELINE=1` and overwrites the baseline.
- `master` / `origin/master`: no `test:evals:baseline:overwrite` script exists.
- `master` / `origin/master`: the harness does not write `test-results/eval-report.log`, report planner iterations, or record per-model usage.

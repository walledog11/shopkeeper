{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build -w packages/db && npm run build -w packages/email && npm run build -w packages/agent && npm run build -w apps/gateway"
  },
  "deploy": {
    "startCommand": "npm run start -w apps/gateway",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
